.instruction-rule-text If two cards have the same color, but the third card has a different color, then it is not a valid set
.trio.false
  .incorrect
  - var cards = [{ attributes: [0,0,2,2] }, { attributes: [1,0,2,1] }, { attributes: [2,1,2,0] }]
  - each card in cards
    include ../card
.trio.false
  .incorrect
  - var cards = [{ attributes: [1,2,0,1] }, { attributes: [1,1,1,1] }, { attributes: [1,1,2,1] }]
  - each card in cards
    include ../card
.trio.false
  .incorrect
  - var cards = [{ attributes: [2,2,1,1] }, { attributes: [0,0,1,1] }, { attributes: [1,2,1,1] }]
  - each card in cards
    include ../card
.trio.false
  .incorrect
  - var cards = [{ attributes: [2,0,2,2] }, { attributes: [0,1,1,2] }, { attributes: [1,1,0,2] }]
  - each card in cards
    include ../card